# 更新日志

本文件记录山海桌面端各版本的更新说明（新增功能 / 优化改进 / 修复问题）。

格式约定：`版本号 - 日期（YYYY-MM-DD）`，条目面向最终用户可读，每条一句话说明「是什么 + 做了什么」。

---

## 0.6.5 - 2026-09-08

**下载**

- Windows x64：`https://store.bjctykj.com/app-versions/Windows/1788797196_Shanhai-0.6.5-x64.exe`
- macOS arm64（Apple Silicon）：`https://store.bjctykj.com/app-versions/macOS/1788798042_Shanhai-0.6.5-arm64.dmg`

**发布时间**：macOS 包 2026-09-08 00:20（以下载地址时间戳与服务器 Last-Modified 为准）；Windows 包 2026-09-08 00:06。

**版本区间说明**：0.6.4 未对外发布过（本机无该版本安装包、仓库内也没有指向它的下载地址），因此本版包含 0.6.4 与 0.6.5 两个版本号期间的全部改动，条目末尾标注对应提交。

### 新增功能

- **消息一键分享给好友**：AI 回复与自己发出的消息卡片底部新增「分享」按钮，点击后选择一位好友即可把这条内容发进私信聊天。`65c978b`
- **管家可以直接找好友发消息**：管家现在能查看你的私信好友名单（昵称、用户名、未读数、最后一条消息时间），你说「跟张三说一声」它不必再向你要编号。`65c978b`
- **创意空间支持应用更新**：应用作者发布新版本后，已安装的应用会在「发现」里标出「有更新」，并提供「更新」按钮，不用再卸载重装。`f483793`
- **更新前的权限确认**：新版本申请的能力比旧版本多时，安装前会列出新旧对比让你确认，不同意就不安装、旧版本原样保留。`f483793`
- **更新后可恢复上一版本**：每次更新会自动保留一份旧版本，可在「我已安装」里一键恢复到更新前的版本。`f483793`
- **管家接管私信**：设置中打开「管家接管私信」后，好友来信可由管家直接回复；首次开启会有确认弹窗，随时可关。`a9c171a`
- **私信消息右键操作**：私信消息支持右键复制、引用到指定会话，方便把聊天内容接到任务里。`a9c171a`
- **未读分割线**：私信会话里标出未读消息的起始位置，回到窗口时一眼看到漏了什么。`a9c171a`
- **输入草稿保留**：每个会话的输入内容单独保存，切换会话或重启应用后不再丢失。`a9c171a`

### 优化改进

- **私信单条消息可写得更长**：客户端的正文长度上限已放宽（约四万字节，相当于上万汉字）；该上限需与配套服务端版本同时上线才能完全生效，服务端未更新时过长消息仍可能被退回。`65c978b`
- **分享与代发消息不再拆成多条**：一条内容整条发出，不加分段编号，便于后续由管家接管处理。`65c978b`
- **内容拦截提示更明确**：消息发不出去时会说明大致是哪一类内容触发了拦截（如疑似密钥、令牌类内容），不再只有一句笼统提示，也不会显示被拦的具体内容。`65c978b`
- **正常技术讨论不再被误拦**：分享或代发消息时，服务器地址、文件路径、程序报错信息不再被当成敏感内容拦截；疑似密钥、令牌、口令与编码数据仍会拦截。`65c978b`
- **私信窗口记住尺寸**：拖动或缩放后，下次打开保持上次的窗口大小。`a9c171a`
- **会话状态更准确**：新增定时与切回窗口时的状态核对，避免会话长时间显示「执行中」或该显示执行中却没有提示。`6cebd0f`
- **欢迎页快捷提问更顺手**：点击推荐问题会把输入框内容替换为该问题，而不是在已有内容后面追加。`6cebd0f`

### 修复问题

- **点「回到桌面」要点两次**：现在点一次即可收起全部山海窗口，只留悬浮图标。`6cebd0f` `65c978b`
- **管家主窗口一直置顶挡住其它应用**：取消强制置顶，悬浮图标仍保持浮在最前。`6cebd0f`
- **询问弹窗内容一多就盖住窗口按钮**：弹窗改为限高并在内部滚动，不再顶出窗口挡住最小化、最大化、关闭按钮。`65c978b`
- **私信「引用到会话」弹层把会话列表挤出去**：被引用消息过长时改为在该区域内滚动，底部会话列表保持完整可点。`65c978b`
- **跨设备已读提示消失后留一条空白**：提示收回时它占据的位置也一起收回，不再留下空白条。`65c978b`
- **分享按钮点了没反应**：修复好友选择层被消息列表容器带到可视区之外的问题，现在点击即在屏幕正中弹出。`65c978b`
- **带图片的消息分享不出去**：修复图片链接被误判为敏感内容导致分享被拦，图片与文字现在一起作为一条消息发出。`65c978b`
- **Windows 上磁吸后拖动一个窗口，另一个窗口自动变大**：修复贴靠、最大化、最小化状态下误调整另一个窗口的问题。`65c978b`
- **个别会话界面偶发整页空白**：修复会话行在编辑状态下组件调用顺序变化导致的渲染中断。`6cebd0f`
- **重试或恢复会话后状态不同步**：补齐缺失的开始与结束事件，让界面状态与实际执行情况一致。`6cebd0f`

### 升级方式

下载上方对应平台的安装包，直接运行覆盖安装即可，历史会话、记忆与已安装应用都会保留，无需卸载旧版本。

### 证据不足 / 待确认

- **Windows 安装包内容未经本机取证**：本机 `release/` 下没有 Windows 产物，无法像 macOS 包那样核对包内代码；macOS 包已实测与线上文件为同一份（大小与校验值一致），且包含本版全部改动条目。
- **私信长度上限依赖服务端**：客户端放宽已随包发布，配套服务端改动尚未部署，二者未同时生效前，过长消息可能被服务端拒绝。
- **macOS 首次打开的放行方式**：本版打包配置了签名与加固运行时，但是否已完成公证未在本机取证，因此不在正式条目里写「需右键打开」之类的引导，待确认后再补。
- **0.6.2 / 0.6.3 的历史条目缺失**：本文件此前只有 0.6.1 一节，0.6.2 与 0.6.3 未写过更新说明；本轮不凭记忆补写，需要时可另起一轮按提交记录整理。

---

## 0.6.1 - 2026-09-03

### 新增功能

- **窗口磁吸与联动移动**：管家窗口拖动靠近会话窗口时自动吸附对齐；吸附后拖动任一窗口，另一个窗口跟随平移保持相对位置；把两个窗口拉开超过阈值后自动解绑、恢复独立移动。
- **「我已安装」应用手动卸载**：创意空间「我已安装」列表的每个应用新增「卸载」按钮，点击后二次确认，确认后从本机移除该插件（卸载不可恢复）。
- **发现面板卡片按钮优化**：本地已安装的应用在卡片上显示「已安装」置灰标记（不再显示「下载安装」按钮），按钮改为紧凑的正常尺寸。

### 修复问题

- **快速切换子会话串台**：修复主进程广播与切换时序的竞态，避免 A 会话窗口显示 B 会话的历史消息与安全模式状态。
- **切到有数据的会话却显示欢迎界面**：修复切换时会话状态补全把已加载历史误清空的问题，切到有历史记录的会话能正确渲染聊天记录。
- **窗口磁吸后拖动不流畅、容易断开**：优化程序性移动判定与解绑阈值/缓冲，让联动更跟手、更不易在快速甩动时误断开。
- **发现面板「已安装」标记失效**：修复插件 id 比对字段错位、以及主进程产物中本地目录读取静默失败两处根因，本地已装插件现在能正确显示「已安装」。
- **下载安装报 404「插件不存在」**：前端改为优先使用插件真实标识，下载安装恢复正常。
- **外网远程（relay）连接网关报 401 且界面无任何提示**：修复登录凭证失效后反复重连刷屏的问题，改为停止无意义重连，并在设置界面明确区分「凭证失效、请重新登录」与其它连接失败提示。
- **我已安装面板「已安装」「卸载」按钮分行显示**：改为同一行显示，按钮紧凑、布局更清爽。

---